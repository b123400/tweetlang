This is an experiment project I made as a trial of Meteor.

It gets tweets from Twitter api and detect language using Bing translate api, then show it in a graph.

URL : http://tweetlang.meteor.com

To start:

```
meteor

```